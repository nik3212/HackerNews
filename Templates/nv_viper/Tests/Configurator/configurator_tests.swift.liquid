{% include 'header' %}

import Quick
import Nimble

@testable import {{ module_info.project_name }}

class {{ prefix }}{{ module_info.name }}ConfiguratorTests: QuickSpec {
    override func spec() {
        let module = {{ module_info.name }}Configurator().configure()

        describe("Check module configuration") {
            it("Module input shouldn't be nil") {
                expect(module).notTo(beNil())
                expect(module).to(beAKindOf({{ module_info.name }}ModuleInput.self))
            }
        }
    }
}
